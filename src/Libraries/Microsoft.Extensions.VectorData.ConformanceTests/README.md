# Microsoft.Extensions.VectorData.ConformanceTests

This package contains conformance tests for `Microsoft.Extensions.VectorData` providers. Provider implementations can use these tests to verify correct behavior and ensure consistency across different vector database backends.
