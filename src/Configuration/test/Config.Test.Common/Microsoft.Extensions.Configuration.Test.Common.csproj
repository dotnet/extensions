<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <!--
      This project does not fit cleanly into well-defined categories. It is neither a test nor an assembly that ships in the product, not a sample or a test asset.
      So these manual settings are required.
    -->
    <IsImplementationProject>false</IsImplementationProject>
    <UseProjectReferences>true</UseProjectReferences>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="Microsoft.Extensions.Configuration" />
    <Reference Include="Microsoft.Extensions.FileProviders.Physical" />
  </ItemGroup>

</Project>
