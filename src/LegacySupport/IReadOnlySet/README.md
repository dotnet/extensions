Enables use of C# IReadOnlySet<T> on older frameworks.

To use this source in your project, add the following to your `.csproj` file:

```xml
<PropertyGroup>
  <InjectIReadOnlySetOnLegacy>true</InjectIReadOnlySetOnLegacy>
</PropertyGroup>
```
