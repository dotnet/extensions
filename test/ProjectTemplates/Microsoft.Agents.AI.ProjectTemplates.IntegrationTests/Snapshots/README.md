# Snapshots

This directory contains verified snapshots of generated template output. These snapshots are used by the template snapshot tests to ensure templates generate expected output.

When template content is added, this directory will contain subdirectories for each test scenario.
